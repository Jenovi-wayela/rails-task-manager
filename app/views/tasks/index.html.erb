<h1>Task List</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= check_box_tag "task_ids[]", task.id %>
      <%= link_to task.title, task_path(task) %>
    </li>
  <% end %>
</ul>
<%=link_to "Add new task", new_task_path%>
